{
  "name": "schools-of-fish",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "ISC",
  "scripts": {
    "start:local": "npm run start:server & npm run start:client",
    "start:server": "cd server && npm run build && npm run start",
    "start:client": "cd client && npm run develop",
    "postinstall": "cd client && npm install && cd ../server && npm install && cd .."
  }
}
